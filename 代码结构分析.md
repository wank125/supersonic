# SuperSonic 项目代码结构分析

## 1. 整体架构概览

SuperSonic 是一个**模块化单体架构**的AI+BI平台，采用Maven多模块管理，包含5个核心模块：

```
supersonic/
├── common/          # 公共基础模块
├── auth/           # 认证授权模块
├── chat/           # 对话功能模块
├── headless/       # 语义层模块
├── launchers/      # 启动器模块
└── webapp/         # 前端React应用
```

## 2. 技术栈分析

### 后端技术栈
- **Java 21** + **Spring Boot 3.3.9**
- **MyBatis Plus 3.5.7** + **PageHelper 6.1.0** (数据访问层)
- **Druid 1.2.24** (数据库连接池)
- **LangChain4j 0.35.0** (AI/LLM集成框架)
- **Calcite 1.37.0** (SQL解析和查询引擎)
- **JWT 0.12.3** (Token认证)

### 数据库支持
- MySQL, PostgreSQL, H2 (主要存储)
- ClickHouse, DuckDB, Trino, Presto (分析引擎)
- Milvus, Chroma, PGVector (向量数据库)

### 前端技术栈
- **React + TypeScript** (主框架)
- **pnpm workspace** (包管理)

## 3. 模块详细分析

### 3.1 Common模块 (`common/`)
**核心共享组件库**，提供：
- **数据库访问层**：MyBatis Plus配置、PageHelper分页
- **SQL处理**：JsqlParser工具类、Calcite集成
- **配置管理**：Spring Boot配置类、线程池配置
- **AI集成**：LangChain4j各种LLM提供商配置
- **工具类**：HTTP客户端、JSON处理、缓存

```
common/src/main/java/com/tencent/supersonic/common/
├── calcite/           # Calcite SQL处理
├── jsqlparser/        # SQL解析工具
├── config/           # 配置类
├── persistence/      # 数据访问层基类
├── service/          # 共享服务
└── pojo/             # 通用数据对象
```

### 3.2 Auth模块 (`auth/`)
**认证授权模块**，采用策略模式：
```
auth/
├── api/              # API接口定义
│   ├── authentication/ # 用户认证接口
│   └── authorization/  # 权限控制接口
├── authentication/   # 认证实现
└── authorization/    # 授权实现
```

**核心功能**：
- 多策略用户认证（HTTP Header、模拟用户等）
- 三级数据权限控制：数据集级、字段级、行级
- JWT Token管理

### 3.3 Chat模块 (`chat/`)
**对话功能模块**，处理自然语言查询：
```
chat/
├── api/              # 对话API接口
└── server/           # 对话服务实现
    ├── service/      # 核心服务
    │   ├── ChatManageService      # 对话管理
    │   ├── ChatQueryService      # 查询处理
    │   ├── AgentService          # 智能代理
    │   └── RecommendService      # 推荐服务
    └── rest/             # REST控制器
        ├── ChatController        # 对话接口
        └── ChatQueryController   # 查询接口
```

### 3.4 Headless模块 (`headless/`)
**语义层核心模块**，最复杂的模块：
```
headless/
├── api/              # 语义层API定义
├── server/           # 语义层服务
│   ├── service/      # 业务服务
│   │   ├── SchemaService        # 模式管理
│   │   ├── MetricService        # 指标管理
│   │   ├── DimensionService     # 维度管理
│   │   └── DataSetService       # 数据集管理
│   └── rest/         # REST控制器
├── core/             # 核心业务逻辑
└── chat/             # 对话相关语义处理
    ├── knowledge/    # 知识库
    └── parser/       # 语义解析器
        ├── llm/      # LLM解析服务
        └── SemanticParser # 语义解析核心
```

**核心组件架构**：
- **Schema Mapper**：自然语言→模式映射
- **Semantic Parser**：用户查询→语义查询（规则+LLM）
- **Semantic Corrector**：语义查询验证和修正
- **Semantic Translator**：语义查询→SQL转换
- **Query Executor**：多数据源查询执行

### 3.5 Launchers模块 (`launchers/`)
**应用启动器模块**：
```
launchers/
├── chat/             # Chat服务启动器
├── headless/         # Headless服务启动器
├── common/           # 启动器公共组件
└── standalone/       # 独立完整服务启动器
```

### 3.6 Webapp前端模块 (`webapp/`)
**React + TypeScript前端**：
```
webapp/
├── packages/
│   ├── chat-sdk/     # 对话SDK
│   └── supersonic-fe/# 主前端应用
├── pnpm-workspace.yaml # pnpm工作空间配置
└── start-fe-*.sh     # 前端启动脚本
```

## 4. 设计模式和架构特点

### 4.1 分层架构
1. **表现层**：REST Controllers (`@RestController`)
2. **业务层**：Service实现 (`@Service`)
3. **API层**：模块间接口契约
4. **持久层**：Repository/MyBatis映射器
5. **领域层**：数据对象和实体

### 4.2 核心设计模式
- **策略模式**：认证、解析器、查询执行器
- **工厂模式**：各种服务创建
- **建造者模式**：复杂查询构建
- **适配器模式**：多数据源适配
- **观察者模式**：数据变更事件

### 4.3 数据流架构
```
用户查询 → Schema Mapper → Semantic Parser → Semantic Corrector
→ Semantic Translator → SQL → Query Executor → 多数据源 → 结果返回
```

## 5. API设计规范

**RESTful API路径规划**：
- `/api/auth/*` - 认证相关接口
- `/api/chat/*` - 对话查询接口
- `/api/headless/*` - 语义层管理接口
- `/api/admin/*` - 管理功能接口

## 6. 配置和部署

### 6.1 Spring Profiles
- `h2` - 开发环境内存数据库
- `mysql`/`postgresql` - 生产数据库

### 6.2 部署模式
- **Standalone模式**：`./assembly/bin/supersonic-daemon.sh start`
- **微服务模式**：独立启动chat或headless服务
- **Docker模式**：`docker-compose up -d`

## 7. 核心服务类清单

### 7.1 REST控制器
- **AuthController** - 认证控制器
- **UserController** - 用户管理控制器
- **ChatController** - 对话控制器
- **ChatQueryController** - 对话查询控制器
- **AgentController** - 智能代理控制器
- **SchemaController** - 模式管理控制器
- **MetricController** - 指标管理控制器
- **DimensionController** - 维度管理控制器
- **DataSetController** - 数据集管理控制器
- **DomainController** - 域管理控制器
- **ModelController** - 模型管理控制器

### 7.2 核心服务类
- **ChatManageService** - 对话管理服务
- **ChatQueryService** - 对话查询服务
- **SchemaService** - 模式服务
- **MetricService** - 指标服务
- **DimensionService** - 维度服务
- **DataSetService** - 数据集服务
- **DomainService** - 域服务
- **ModelService** - 模型服务
- **AuthService** - 认证服务
- **UserService** - 用户服务
- **EmbeddingService** - 嵌入服务
- **LLMRequestService** - LLM请求服务
- **LLMResponseService** - LLM响应服务

## 8. 数据库架构

### 8.1 核心实体关系
```
Domain (业务域)
├── Model (语义模型)
│   ├── Metric (指标)
│   └── Dimension (维度)
└── DataSet (数据集)
    ├── 数据权限配置
    └── 查询规则
```

### 8.2 MyBatis Plus配置
- 使用`@TableName`注解映射表名
- 自动分页配置PageHelper
- Repository模式数据访问抽象
- 数据对象(DO)模式

## 9. AI/LLM集成架构

### 9.1 支持的LLM提供商
- OpenAI GPT系列
- Azure OpenAI
- 本地AI服务
- Ollama
- 千帆、智谱AI、DashScope
- ChatGLM

### 9.2 向量数据库支持
- Chroma
- Milvus
- PGVector
- OpenSearch

### 9.3 嵌入模型
- BGE Small ZH
- MiniLM L6 V2
- 可自定义嵌入模型

## 10. 开发和构建

### 10.1 Maven构建命令
```bash
# 完整构建（跳过测试）
mvn clean package -DskipTests -Dspotless.skip=true

# 代码格式化
mvn spotless:apply

# 运行测试
mvn test

# 构建特定模块
mvn clean package -pl headless/core
```

### 10.2 服务启动
```bash
# 启动独立服务（包含所有模块）
./assembly/bin/supersonic-daemon.sh start

# 启动特定服务
./assembly/bin/supersonic-daemon.sh start chat
./assembly/bin/supersonic-daemon.sh start headless

# 停止服务
./assembly/bin/supersonic-daemon.sh stop
```

### 10.3 前端开发
```bash
cd webapp
npm install
npm run start    # 开发服务器
npm run build    # 生产构建
```

## 11. 项目特点总结

1. **模块化设计**：清晰的模块边界，便于维护和扩展
2. **多数据源支持**：统一接口支持多种数据库和数据引擎
3. **AI优先**：深度集成LangChain4j，支持多种LLM
4. **权限完善**：三级权限控制保障数据安全
5. **部署灵活**：支持单体、微服务、Docker等多种部署方式
6. **开发友好**：完善的开发工具链和自动化配置

这是一个设计良好的企业级AI+BI平台，采用清晰的模块化架构，支持多种部署方式和数据源，具有良好的扩展性和维护性。项目体现了现代Java应用的最佳实践，是学习企业级架构设计的优秀案例。